.comment-box {
  display: flex;
  flex-direction: column;
  font-size: 1.2em;
  justify-content: flex-start;
  flex: 1;
  max-height: 100%;
  overflow: scroll-y;
}

.container {
  display: flex;
  text-align: right;
  padding: 1em;
  flex-direction: column;
  height: 100%;
  box-sizing: border-box;
  & > * {
    margin-bottom: 0.5em;
    width: 100%;
  }
}

.align-r {
  text-align: right
}
