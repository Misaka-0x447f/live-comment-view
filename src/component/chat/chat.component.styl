.container {
  font-size: 1.6em;
  display: flex;
  align-items: center;
}

.breadcrumb {
  font-size: 0.5em;
  margin-right: 0.5em;
  display: flex;
  flex-direction: column;
  text-align: right;
  line-height: 0.95em;
}
