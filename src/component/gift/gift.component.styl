.container {
  display: flex;
  flex-direction: column;
}

.unit {
  margin-right: 0.1em;
  font-size: 0.6em;
}

.total {
  font-size: 3em;
  width: 100%;
  display: flex;
  justify-content: flex-end;
  align-items: baseline;
}

.list {
  font-size: 1.2em;
}
