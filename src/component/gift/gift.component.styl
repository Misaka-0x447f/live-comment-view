.container {
  display: flex;
  flex-direction: column;
}

.data {
  display: flex;
  align-items: baseline;
}

.unit {
  margin-right: 0.1em;
  font-size: 0.6em;
}

.price {
  display: flex;
  align-items: baseline;
}

.status {
  font-size: 3em;
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: baseline;
}

.list {
  font-size: 1.2em;
}
